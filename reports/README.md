# Experiment artifacts

This folder is populated by `run_all_experiments.py`.

- `eda/`: numeric summaries and family stratification tables.
- `metrics/`: stage-level metrics with dataset/model/scenario tags.
- `predictions/`: prediction tables with true/predicted values and tags.
- `validation/`: association-rule outputs and property deviation summaries.
